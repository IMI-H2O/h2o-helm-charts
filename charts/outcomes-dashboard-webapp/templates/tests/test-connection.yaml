apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "outcomes-dashboard-webapp.fullname" . }}-test-connection"
  labels:
    {{- include "outcomes-dashboard-webapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "outcomes-dashboard-webapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
